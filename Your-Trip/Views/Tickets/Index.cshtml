@model IEnumerable<YourTrip.Models.Ticket>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.LandingDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TakeOffTime)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LandingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TakeOffTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TicketNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TicketNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TicketNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
